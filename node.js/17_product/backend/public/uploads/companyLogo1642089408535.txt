util 抽离

日志：开发者 console  用户 localstoreage , 上传用户服务器

生命周期视频模块完成

完成对接需要演示什么步骤

改造class 保留main

支持上架npm

camera + desktop || 互动模块 = video

根据身份异步加载管理模块（后端或者前端传入身份，或者初始化传入标志）

看看生活直播和大班有些功能归回sdk，比如点赞等

根据配置异步加载功能模块

sdk初始化参数（支持用户传入字段读取）

考虑开放一些模块，比如商品管理（举例）

二、
2022 观望：
公司sdk上架npm  gitee
videoPlayer ui集成在sdk中（弹幕等）